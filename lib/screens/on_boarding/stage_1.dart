import 'package:flutter/material.dart';

class StageOne extends StatelessWidget {
  const StageOne({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('stage one'),
    );
  }
}
